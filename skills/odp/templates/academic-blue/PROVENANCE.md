# Provenance: academic-blue

- **Source**: hand-curated for `open-document-skills`
- **Author**: open-document-skills contributors
- **License**: MIT (see `LICENSE.txt`)
- **Design language**: cream background (`#FDFBF6`), Lato sans-serif headings
  in deep navy (`#1B3A57`), Source Serif body text in near-black (`#2B2B2B`).
  Easy on the eye for long talks; reads well projected. No logo by default —
  add one by editing `Default` master-page in `styles.xml` or via
  `customize_master.py`.
