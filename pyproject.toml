[project]
name = "open-document-skills"
version = "0.1.0"
description = "Codex skills for OpenDocument Format files"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
